# 核心链路验收记录（2026-09-15）

目标：验证“Android App 数据上传 → 社区素材 → 日记 → 游记”的核心过程，不只依赖种子数据。

## 当前结论

已通过后端真实接口跑通完整闭环：登录、上传图片、同步 App 素材、查询素材、保存日记、AI 批量生成日记、生成游记、查询游记。

本次验收使用后端：`http://localhost:3001`。

## 本次真实生成的数据

- 登录账号：`zhangshan`（种子账号登录成功）
- 上传图片：`/uploads/images/672ad76a-f6a8-4b6c-aa15-a61513ec797e.png`
- 同步结果：`imported = 2`，`skipped = 0`
- 图片素材：`bd03490b-c5f7-404f-8932-91a7d29e53c6`
- 纯文字素材：`2d406ae5-9bcd-40dc-b9f6-6e161a0c599c`
- 手动保存日记：`a000fb7d-177f-4c12-b8c1-842316657637`
- AI 批量生成日记：`8d50563d-b48b-4137-8b4a-120dc8eace48`
- 生成游记：`9c24db9a-708e-4e5c-bb74-6a643fce04e9`

## 验收覆盖

1. App 上传素材
   - 已通过 `POST /api/upload/image` 上传真实文件。
   - 返回了可用于社区侧素材的 `url`。

2. App 快照同步到社区素材
   - 已通过 `POST /api/sync/snapshots` 同步 2 条 moments。
   - 一条带图片，一条为无图片/无视频的纯文字素材。
   - `GET /api/posts/snaps` 能查到这 2 条素材。
   - 图片素材带 `IMAGE` media item。
   - 纯文字素材带 `vrMetadata.mediaType = TEXT`，说明文字素材不会被漏掉。

3. 素材生成/保存日记
   - 已通过 `POST /api/posts/diary/save` 从图片素材保存日记。
   - `GET /api/posts/diaries` 能查到该日记。
   - 已通过 `POST /api/posts/diary/generate-batch` 对 2 条素材生成 AI 日记，job 状态为 `DONE`。

4. 素材/日记生成游记
   - 已通过 `POST /api/posts/travelogue/generate` 使用 2 条素材 + 1 篇日记生成游记。
   - `GET /api/posts/travelogue/job/:jobId` 返回 `DONE` 且有 `postId`。
   - `GET /api/posts/travelogues` 能查到生成的游记。

## 前端浏览入口只读检查

当前前端有对应入口和 API 调用：

- 素材浏览：`web/src/app/(main)/snap/page.tsx`，调用 `GET /posts/snaps`
- 多素材生成日记：`web/src/app/(main)/snap/generate/batch/page.tsx`，调用 `POST /posts/diary/generate-batch`
- 日记列表/详情：`web/src/app/(main)/diaries/page.tsx`、`web/src/app/(main)/diaries/[id]/page.tsx`
- 日记保存：`web/src/app/(main)/diaries/new/page.tsx`，调用 `POST /posts/diary/save`
- 游记生成：`web/src/app/(main)/journeys/generate/page.tsx`，调用 `POST /posts/travelogue/generate`
- 游记列表/详情：`web/src/app/(main)/journeys/page.tsx`、`web/src/app/(main)/journeys/[id]/page.tsx`
- API 封装：`web/src/lib/snap-api.ts`


## 页面级验收补充

已用 `scripts/verify-ui-browse.mjs` 注入登录态后访问本地前端 `http://localhost:3000`：

- `/snap` 加载成功，未跳登录，未出现前端运行时错误；页面显示“今天 2 张”，对应本次同步的 2 条素材。
- `/diaries` 加载成功，未跳登录，未出现前端运行时错误；页面显示日记统计，能承接本次生成/保存的数据。
- `/journeys` 加载成功，未跳登录，未出现前端运行时错误；切换到“我的（私密）”后能看到刚生成的《江风与数据之间：一个人的重庆链路验收记》。
- 浏览过程中核心内容接口未出现 4xx/5xx。
## Android 侧已完成的连接修复

Android 项目路径：`C:\Users\33306\OneDrive\Desktop\徐霞客\数据\Kuaishan-Android-Source-0.8.2-20260724`

已修复：

- Android 默认社区后端地址改为当前 WLAN 可访问地址：`http://192.168.31.122:3001`
- 支持通过 Gradle 属性 `COMMUNITY_BASE_URL` 覆盖后端地址。
- App 查询待同步 moments 时纳入纯文字 note。
- 同步逻辑不再跳过纯文字/音频反思素材。
- 素材上传成功但 `/api/sync/snapshots` 未成功时，不再提前标记为已同步。
- Android Debug 包构建通过：`./gradlew.bat :app:assembleDebug`。

## 后续仍建议继续做的事

1. 用 Android 真机或模拟器点一次“同步到社区”，验证手机网络能访问 `http://192.168.31.122:3001`。
2. 启动前端后，用浏览器登录 `zhangshan/password123`，肉眼检查 `/snap`、`/diaries`、`/journeys` 是否展示刚刚生成的数据。
3. 如果要首发给他人试用，把本地 IP 改为局域网稳定地址或部署地址，避免 Android 包内后端地址失效。
4. 对游记/日记的“发布到社区广场”做下一步验收，因为本次核心链路生成的数据默认是私密内容。

