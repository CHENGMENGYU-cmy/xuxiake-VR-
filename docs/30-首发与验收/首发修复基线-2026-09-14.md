# 徐霞客社区首发修复基线

记录日期：2026-09-14。用途：开始按 `首发收口实施计划-2026-09-14.md` 逐项实现前，固定当前工作区边界。

## 1. 当前状态分组

| 类别 | 文件 | 处理建议 |
|---|---|---|
| 应纳入首发版本的代码改动 | `server/src/**`、`web/src/**`、`web/e2e/**`、`server/test/**` 中与功能和测试有关的修改 | 修复完成并通过检查后纳入版本 |
| 应纳入首发版本的SQL | `server/sql/*.sql` 中迁移、清理和真实图片切换脚本 | 标注执行顺序，禁止无说明重复播种 |
| 应纳入首发版本的文档 | `../../CHANGELOG.md`、`../00-入口与交接/AI_HANDOFF.md`、`../00-入口与交接/项目开发交接记录.md`、`docs/**` | 保持“历史记录”和“当前验证”分离 |
| 需要独立交付的媒体 | `server/uploads/real-covers/**`、来源清单 | 因 `server/uploads/` 被忽略，必须有恢复说明或独立资产包 |
| 本机备份和临时产物 | `backup-xuxiake-before-*.sql`、`current-community-images-contact-sheet.jpg`、运行截图和日志 | 不直接纳入首发代码版本 |
| 本机配置 | `.codex/config.toml` | 不纳入项目版本 |

## 2. 修复起点

- 最新已确认问题入口：`项目现状与问题清单-2026-09-14.md`。
- 当前实施计划：`首发收口实施计划-2026-09-14.md`。
- 首轮优先范围：T01-T05，即版本边界、账号生命周期、认证验证码、权限测试、真实搜索。

## 3. 本轮执行规则

- 不回滚已有未提交改动。
- 不重播历史清理或真实图片SQL，除非当前任务明确需要。
- 数据库结构变化通过新增迁移SQL表达，执行前先确认目标库。
- 每关闭一个问题，要同步代码、测试和对应文档状态。
