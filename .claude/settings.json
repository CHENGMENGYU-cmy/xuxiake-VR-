{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (FILES=$(git diff --cached --name-only | head -3 | tr '\\n' ', ' | sed 's/,$//') && git commit -m \"auto: update $FILES\")"
          }
        ]
      }
    ]
  }
}
