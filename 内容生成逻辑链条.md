# 徐霞客 内容生成逻辑链条（完整版）

> 版本：V1.2 · 2026-08-14
> 用途：从"手机闪拍数据采集"到"社区内容发布"的完整链路说明，供后续 App 端开发、接口联调、功能扩展对照使用。
> V1.2 变更：内容链条简化为3级（去掉LOG层）；闪拍支持文字备注(textNote)；AI游记生成器改为从闪拍+日记生成
> V1.1 变更：游记升级为「图文叙事章节式」（手写 + AI 结构化统一）；内容治理（软删除 + 统计口径）

---

## 总览图

```
┌────────────────────────────────────────────────────────────────────────┐
│ 第一层：数据采集（闪拍 App）                                              │
│                                                                            │
│  AI 眼镜 → 拍照 / 录像 / 语音感悟                                           │
│     │                                                                    │
│     ├─ 硬件元数据：时间、GPS、地点名、天气                                  │
│     ├─ 语音感悟 → 阿里云 NLS 转写为文字                                     │
│     └─ 生成三份本地数据：                                                  │
│         ① 拍摄票据 (SharedPreferences JSON)                                │
│         ② noah_travel.db (SQLite: trips/moments/reflections)              │
│         ③ 物理媒体 (Noah/Photos/*.jpg, *.mp4, Thumbnails/*.jpg)           │
└────────────────────────────┬───────────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 第二层：数据同步（App → 社区后端）✅ 已实现                               │
│                                                                            │
│  ① 上传媒体文件                                                           │
│     POST /api/upload/image | video | audio   →  拿到 /uploads/xxx  URL   │
│                                                                            │
│  ② 提交元数据                                                            │
│     POST /api/sync/snapshots  (Bearer token)                             │
│     { trips[], moments[], reflections[], activityCategories[] }          │
│        │                                                                │
│        ├─ 按 originalId 幂等去重 → 不重复插入                              │
│        ├─ 每条 moment → 创建 SNAPSHOT 闪拍 (PRIVATE 仅自己可见)            │
│        ├─ 媒体 URL → media_items（支持纯文字闪拍，无媒体也创建帖子）      │
│        ├─ textNote + recognizedText → 闪拍正文 content（手动备注优先）    │
│        └─ GPS/天气/活动分类/source/hasTextNote → vr_metadata JSON         │
└────────────────────────────┬───────────────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 第三层：内容分层（我的内容 · 侧边栏）                                       │
│                                                                            │
│  📷 素材库     contentLevel=SNAPSHOT   闪拍素材（照片+视频+文字记录）     │
│        │  每条闪拍可包含：照片/视频 + GPS/天气 + 文字备注/语音感悟         │
│        ▼ 点击卡片                                                        │
│  📝 我的日记   contentLevel=DIARY    情感反思文章                        │
│        │  AI 生成（5种风格） / 手写 / 从闪拍生成                           │
│        │                                                              │
│        ▼ 保存为 草稿 / 私密 / 公开                                      │
│  📖 我的游记   contentLevel=TRAVELOGUE  最终产出物（图文叙事章节式）      │
└────────────┬───────────────────────────────┬───────────────────────────┘
             │                               │
             ▼                               ▼
┌───────────────────────────┐   ┌──────────────────────────────────────┐
│ 第四层：AI 生成 ✅ 已实现   │   │ 第五层：发布 ✅ 已实现                 │
│                           │   │                                        │
│  A. 日记生成               │   │  A. 日记                              │
│    POST /posts/diary/generate│  │    ├─ 草稿  → 我的日记(私密)          │
│    输入: 1条闪拍 + 风格      │   │    ├─ 私密  → 仅自己可见              │
│    输出: DIARY 标题/正文/感悟 │  │    └─ 公开  → 日记广场                 │
│                           │   │                                        │
│  B. 游记生成               │   │  B. 游记                              │
│    POST /posts/travelogue/ │   │    ├─ 保存私密 → 游记散文-我的          │
│        generate            │   │    └─ 发布社区 → 游记散文-已发布        │
│    输入: N条闪拍 + N条日记   │   │   编辑: 章节式图文编辑器统一            │
│         + 提示词 + 语气篇幅 │  │   可见性切换即时刷新两区                  │
│    输出: 结构化 TRAVELOGUE  │   │                                        │
│    （按天分章+配图+信息卡）  │   │                                        │
└───────────────────────────┘   └──────────────────────────────────────┘
```

---

## 各层详细说明

### 第一层：数据采集（闪拍 App 本地）

| 数据 | 存储位置 | 内容 |
|---|---|---|
| 拍摄票据 | SharedPreferences (Gson JSON) | `id`, `capturedAt`, `mediaType`, `photoPath`, `durationMs`, `activityCategoryId`, `categorySource`, `categoryLocked` |
| 数据库 | `noah_travel.db` (SQLite) | `trips`, `travel_moments`, `reflections`, `metadata_tasks`, `activity_categories` |
| 媒体 | `Noah/Photos/`, `Noah/Thumbnails/` | 原始 `.jpg` `.jpeg` `.png` `.mp4` `.mov` 二进制 |

**闪拍 App 数据结构（示例）**

拍摄票据 JSON：
```json
{
  "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "capturedAt": 1690435500000,
  "source": "GLASSES_BUTTON",
  "status": "COMPLETED",
  "mediaType": "PHOTO",
  "photoPath": "/storage/.../Noah/Photos/20260804151230123.jpg",
  "videoPath": null,
  "thumbnailPath": null,
  "recordingEndedAt": null,
  "durationMs": null,
  "activityCategoryId": "unclassified",
  "categorySource": "NONE",
  "categoryLocked": false,
  "error": null
}
```

SQLite 五张表：
| 表名 | 说明 |
|---|---|
| `trips` | 旅行记录（id, title, startTime, endTime, status） |
| `travel_moments` | 旅行瞬间（核心：照片/视频路径、GPS、地点、天气、活动分类） |
| `reflections` | 语音/文字感悟（recognizedText, audioPath, status） |
| `metadata_tasks` | 地点/天气补全任务（taskType, status, retryCount） |
| `activity_categories` | 活动分类（displayName, iconKey, sortOrder） |

### 第二层：数据同步（App → 社区后端）✅ 已实现

**为什么分两步？**
- 媒体文件太大 → 走分片上传接口，拿到 URL
- 元数据很小 → 直接 JSON POST

**Step 1：上传媒体文件**

| 类型 | 接口 | 返回 |
|---|---|---|
| 图片 | `POST /api/upload/image` | `{ url: "/uploads/images/xxx.jpg" }` |
| 视频 | `POST /api/upload/video` | `{ url: "/uploads/videos/xxx.mp4" }` |
| 音频 | `POST /api/upload/audio` | `{ url: "/uploads/audio/xxx.mp3" }` |

**Step 2：提交元数据**

```
POST /api/sync/snapshots
Authorization: Bearer <token>
Content-Type: application/json
```

请求体（对应 App 的 SQLite + 票据数据）：
```json
{
  "trips": [{ "id": "trip-001", "title": "漓江之旅", "startTime": 1690435500000, "endTime": 1690437000000, "status": "COMPLETED" }],
  "moments": [
    {
      "id": "moment-001",
      "capturedAt": 1690435500000,
      "mediaType": "PHOTO",
      "photoPath": "/uploads/images/xxx.jpg",
      "thumbnailPath": "/uploads/images/xxx_thumb.jpg",
      "videoPath": null,
      "durationMs": null,
      "activityCategoryId": "photography",
      "categorySource": "MANUAL",
      "categoryLocked": true,
      "source": "GLASSES_BUTTON",
      "gpsLat": 24.78,
      "gpsLng": 110.49,
      "locationName": "桂林阳朔",
      "weather": "晴",
      "keywords": ["漓江", "日出"],
      "textNote": "在漓江边站了十五个小时等光",
      "mood": "平静而满足",
      "scene": "摄影"
    }
  ],
  "reflections": [
    { "momentId": "moment-001", "recognizedText": "在漓江边站了十五个小时...", "audioPath": "/uploads/audio/xxx.mp3", "status": "COMPLETED" }
  ],
  "activityCategories": [
    { "id": "photography", "displayName": "摄影", "iconKey": "camera", "sortOrder": 1 }
  ]
}
```

响应：
```json
{ "success": true, "data": { "imported": 3, "skipped": 0 } }
```

**同步逻辑**：
- 幂等：按 `moment.id` 存入 `vr_metadata.originalId`，重复同步跳过
- 每条 moment → 创建 1 条 `SNAPSHOT` 帖子（`visibility=PRIVATE`）
- 媒体 → 关联 `media_items` 表（IMAGE/VIDEO/AUDIO）
- 感悟文字 → 作为闪拍正文 `content`

**实现文件**：
- `server/src/modules/sync/sync.controller.ts` — `POST /api/sync/snapshots`
- `server/src/modules/sync/sync.service.ts` — 映射逻辑
- `server/src/modules/sync/sync.module.ts`
- `web/src/lib/snap-api.ts` — `syncSnapshots()` 前端封装
- `scripts/test-sync-snapshots.js` — 接口测试脚本

**App 端上传调用（Android 侧，✅ 已实现）** — 闪拍 App 工程 `com.noah.glassesjourney.data`：
- `CommunitySyncManager.kt` — 同步协调器（登录 / 上传 / 幂等 / token 刷新）
- `CommunityBackendClient.kt` — 社区 API 客户端（验证码登录 / 上传媒体 / sync）
- `CommunityTokenStore.kt` — token 与已同步 id 本地持久化（按账号隔离）
- `ui/SyncToCommunityActivity.kt` — “同步到社区”页面（账号+密码+图形验证码登录，手动触发同步）
- 入口：主界面“同步到社区”按钮；`COMMUNITY_BASE_URL` 在 `app/build.gradle.kts` 配置

### 第三层：内容分层（侧边栏"我的内容"）

核心信息架构 —— 每个入口对应唯一内容层级：

| 侧边栏入口 | contentLevel | 功能 | 可见性 |
|---|---|---|---|
| 素材库 | `SNAPSHOT` | 闪拍素材（照片+视频+文字记录），分类筛选（地点/时间），点击 AI 生成日记 | 仅自己 |
| 日志分类 | `LOG`（兼容旧数据） | 按维度浏览日志内容 | 仅自己 |
| 我的日记 | `DIARY` | 情感反思，AI 生成/手写 | 草稿/私密/公开 |
| 我的游记 | `TRAVELOGUE` + `ESSAY` | AI 综合生成/手写的最终文章 | 私密/公开 |
| 日记广场（浏览区） | `DIARY` 公开 | 浏览所有人的公开日记 | 公开 |

**闪拍 vs 日记 vs 游记（3级链条）**

| | 闪拍 SNAPSHOT | 日记 DIARY | 游记 TRAVELOGUE |
|---|---|---|---|
| 本质 | 随手记录的一切 | 情感反思文章 | 综合长文 |
| 内容 | 照片+视频+文字备注+语音（可选组合） | 情感文字+配图 | 结构化章节+配图+信息卡 |
| 来源 | App 同步（自动采集+手动备注） | AI 生成/手写 | 闪拍+日记+AI |
| 发布 | 否（仅自己可见） | 可发布 | 可发布 |
| 作用 | 原始素材 | 内容 | 产出物 |

### 第四层：AI 生成 ✅ 已实现

**A. 日记生成**（单条闪拍 → 日记）

```
POST /api/posts/diary/generate
{ "snapId": "...", "style": "温柔治愈风", "includeMemory": false }
```

- 5 种风格：温柔治愈风 / 生活碎片风 / 成长复盘风 / 诗意散文风 / 轻松口语风
- 支持"回忆反差"：系统找到相似历史日记做对比
- 实现：`server/src/modules/posts/diary-generator.service.ts`

**B. 游记生成**（多条闪拍素材 + 日记 + 提示词 → 结构化章节式游记）

```
POST /api/posts/travelogue/generate
{
  "snapIds": ["..."],
  "diaryIds": ["..."],
  "prompt": "重点写漓江日出过程",
  "style": "游记",
  "tone": "温暖",
  "length": "标准"
}
```

- 语气：纪实 / 温暖 / 轻松
- 篇幅：简短(500字) / 标准(1000字) / 详细(2000字)
- 引擎：`AiClientService` → DeepSeek API（真实 AI）
- 失败自动回退到本地模板生成
- **素材来源**：闪拍提供事实骨架（GPS/天气/时间/文字备注），日记提供情感血肉（感受/反思）
- **结构化生成**：后端按素材 `createdAt` 自动分"天"，每天一个章节（Day N），章节标题=地点+日期、正文=该日素材的事实+感悟、**自动配当天素材照片**；同时生成标题 / 导语 / 信息卡（目的地、出行方式、人均、主题）/ 结尾感悟 / 推荐亮点 / 实用贴士
- **落库结构**：创建 `TRAVELOGUE` 帖子 + `journey`（含 summary/transport/budget/theme/insight/tips）+ `journey_stops`（每天一个 stop，含 dayDate/locationName/description/highlights/tips）+ `journey_stop_media`（每站多图）；`postType` 统一为 `JOURNEY`
- **生成后落回编辑器**：结果跳转 `/upload/journey-creator?edit=:postId`，在章节式编辑器中继续手改章节文字 / 增删图 / 改信息卡后再发布
- 实现：`server/src/modules/posts/ai.service.ts`

**C. 手写游记**（`/upload/journey-creator`，章节式图文编辑器）

- 顶部：标题 + 封面 + 导语；信息卡：目的地 / 起止日期 / 出行方式 / 人均 / 主题
- 主体：按 Day 添加章节，每章 = 日期 + 地点 + 正文 + **多图**（本地上传 / 从素材库选图）
- 底部：结尾感悟
- 发布 `contentLevel=TRAVELOGUE`（与 AI 生成统一层级），编辑模式完整回填全部字段（含出行方式/主题自由文本）

**AI 配置（server/.env）**
```env
AI_PROVIDER=deepseek
AI_API_KEY=sk-xxx
AI_MODEL=deepseek-chat
AI_BASE_URL=https://api.deepseek.com/v1
```

**AI 生成原则**（需求文档约束）：
- 闪拍提供事实骨架（时间/地点/天气/文字备注），日记提供情感血肉（感受/反思）
- 基于真实素材写作，不虚构地点、人物、事件
- 素材库「按行程」不再直接生成游记（避免"闪拍跳级为游记"，游记统一从日记/游记层生成）

### 第五层：发布 ✅ 已实现

**A. 日记发布**

| 操作 | 可见性 | 结果 |
|---|---|---|
| 保存草稿 | PRIVATE | 我的日记 |
| 保存为私密 | PRIVATE | 我的日记 |
| 发布到广场 | PUBLIC | 日记广场 |

**B. 游记发布**

| 操作 | 可见性 | 结果 |
|---|---|---|
| 保存为私密 | PRIVATE | 游记散文 → 我的（私密） |
| 发布到社区 | PUBLIC | 游记散文 → 已发布 |

- 可见性切换即时刷新"已发布/私密"两个分区
- 帖子卡片按 `contentLevel` 显示标签：日志 / 日记 / 游记
- 详情页按章节图文叙事渲染（封面 + 导语 + 信息卡 + Day 章节图墙 + 结尾感悟）；列表页卡片显示封面 + 标题 + 目的地/天数徽标
- 编辑：详情页「编辑」→ 章节式图文编辑器

### 第六层：内容治理 ✅ 已实现（2026-08-08）

**软删除**（posts 表 `deleted_at`）
- 删除改为**逻辑删除**：`deletePost` 置 `deleted_at`，不再物理删除，内容可恢复、可审计
- 所有 posts 读查询统一过滤 `deleted_at IS NULL`（feed / 详情 / 层级列表 / 用户日记·游记·闪拍·日志 / 日记广场 / 合集 / 分类维度 / 行程）

**统计口径**
- 个人主页「帖子数」= 排除闪拍/日志素材 + 排除草稿（`vrMetadata.status=draft`）+ 排除软删的有效内容
- 草稿单独在「我的日记」草稿箱管理，不计入作品数、不出现在个人主页
- 删除日记/游记后，个人主页即时同步

---

## 当前完整度与后续缺口

---

## 当前完整度与后续缺口

| 环节 | 状态 |
|---|---|
| App 数据采集 | ✅ 已有（闪拍 App） |
| 媒体上传接口 | ✅ 已有 |
| 元数据同步接口 | ✅ 已实现+测试通过 |
| 内容分层 | ✅ 已调整 |
| 日记 AI 生成 | ✅ 真实 AI |
| 游记 AI 生成（结构化章节式） | ✅ 真实 AI + 按天分章/配图/信息卡 |
| 手写游记（章节式图文编辑器） | ✅ 已实现 |
| 发布/可见性 | ✅ 已实现 |
| 内容软删除 | ✅ 已实现（deleted_at，可恢复） |
| 统计口径（排除草稿/软删/素材） | ✅ 已实现 |
| **App 端上传调用代码** | ✅ 已实现（Android 端”同步到社区”，手动触发） |
| **闪拍文字备注(textNote)** | ✅ 后端已支持，App 端待增加字段 |
| **3级内容链条（去掉LOG层）** | ✅ 已简化为 闪拍→日记→游记 |
| **游记生成器使用闪拍素材** | ✅ snapIds 替代 logIds |
| **网页导入工具** | ❌ 可选（手动导入入口） |

---

## 关联需求文档

- 闪拍 AI 游记与社区系统需求说明书：`c:\Users\33306\OneDrive\Desktop\徐霞客\数据\快闪_AI游记与社区系统需求说明书_V1.0.md`
