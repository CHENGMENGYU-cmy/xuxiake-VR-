# 徐霞客社区建设实现说明

> 基于会议记录"AI时代社交平台"建设意见 + 快闪AI游记需求说明书 + 现有代码基础
> 编制日期：2026-07-27

---

## 一、社区建设路线图

### 整体架构：从"传统社交"到"AI时代第一视角平台"

```
用户（"徐霞客"）→ 眼镜/手机记录 → 四层内容体系 → 社区展示 → 搭子匹配 → 持续互动
                         │                  │              │            │
                    ① 瞬间捕获      ② 内容分类      ③ 日记       ④ 游记散文
                   (第一视角)     (自动归类)     (个人随笔)    (正式文章)
```

### 实施阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 品牌升级 | 全站UI/文案统一为"第一视角""瞬间捕获"主题 | ✅ 已完成 |
| 四层内容体系 | 数据模型 + 后端API + 四个前端页面 | ✅ 已完成 |
| 搭子系统 | 匹配算法 + 推荐API + 前端找搭子页面 | ✅ 已完成 |
| 社区首页改版 | 发现/关注双模式 + 类型筛选 | ✅ 已完成 |
| **第一版：四层内容闭环** | 分类过滤 + 日记写存 + 游记创作 + 发布控制 | ✅ 已完成 |
| AI游记生成 | 多模态模型接入 + 内容生成管线 | 后续版本 |
| 内容审核 | 敏感词检测 + 审核队列 + 举报系统 | ✅ 已完成 |
| 微信分享 | 系统分享+海报+JS-SDK | 后续版本 |

---

## 二、品牌升级实现细节

### 2.1 命名对照表

| 原名称 | 新名称 | 出现位置 |
|---|---|---|
| 旅行视频 | 第一视角 / 瞬间捕获 | 侧边栏、上传页、媒体页、探索页 |
| 旅行图片 | 影像集 / 瞬间捕获 | 侧边栏、上传页、媒体页、探索页 |
| 音频记录 | 语音记录 | 侧边栏、上传页、媒体页、搜索建议 |
| 媒体广场 | 第一视角 / 瞬间捕获 | 侧边栏分区、媒体页标题 |
| VR内容 / VR | 第一视角 | 帖子卡片标签、Feed筛选、聊天卡片 |
| 上传内容 | 分享见闻 | 侧边栏、上传页标题 |
| 笔记 | 随记 | 帖子卡片、Feed筛选 |
| 动态 | 瞬间 | 帖子卡片、Feed筛选、探索页 |
| 发现好友 | 找搭子 | 侧边栏 |
| 发布内容（个人主页） | 在路上 | Profile Tab |
| 媒体库（个人主页） | 瞬间捕获 | Profile Tab |
| 赞过的（个人主页） | 收藏 | Profile Tab |
| 旅程 | 游记散文 | journeys页面 |
| 上传（移动端） | 分享 | 移动端底部导航 |

### 2.2 侧边栏布局

```
┌─────────────────────┐
│    用户信息          │
├─────────────────────┤
│ 浏览发现             │
│ 首页 / 探索 / 话题   │
│ 找搭子              │
├─────────────────────┤
│ 我的内容             │
│ 内容分类 / 日记 / 游记│
├─────────────────────┤
│ 创作                 │
│ 分享见闻             │
├─────────────────────┤
│ 个人                 │
│ 消息 / 通知 / 设置   │
└─────────────────────┘
```

**设计原则：**
- 所有菜单项统一 ghost button 风格，与整体设计一致
- "浏览发现"：公开内容探索，瞬间捕获功能已合并到探索发现页
- "我的内容"：个人内容管理，聚焦分类整理、日记、游记
- "创作"：分享见闻入口
- "个人"：消息/通知/设置
- 整体4个分区，风格统一

### 2.3 Feed首页改造

- 新增 **发现/关注** 双模式切换（默认"发现"展示全站公开内容）
- 内容类型筛选标签调整为：全部 / 第一视角 / 随记 / 游记 / 路线 / 攻略 / 瞬间

---

## 三、四层内容体系实现

### 3.1 数据模型

```sql
-- posts 表字段
content_level ENUM('SNAPSHOT','CLASSIFIED','DIARY','ESSAY')
    NOT NULL DEFAULT 'SNAPSHOT' COMMENT '内容层级',
parent_post_id VARCHAR(36) NULL COMMENT '上级内容ID',

-- content_level 说明：
-- SNAPSHOT   ① 瞬间捕获：眼镜/手机的原始第一视角画面
-- CLASSIFIED ② 内容分类：按地点/时间/类型自动归类，不带主观色彩
-- DIARY      ③ 日记/日志：内容+部分感想，带有个人色彩但未形成正式文章
-- ESSAY      ④ 游记散文：基于前三层，有个人情感的正式文章

-- parent_post_id：自引用外键，形成内容升级链路
-- 例如：SNAPSHOT(原始照片) → DIARY(带感想的日记) → ESSAY(正式游记)
```

### 3.2 后端API

#### 内容层级查询
```
GET /api/posts/hierarchy?level=SNAPSHOT&userId=xxx&cursor=yyy&limit=12
```
按内容层级获取帖子列表，支持游标分页。

#### 分类维度聚合
```
GET /api/posts/classified/dimensions?userId=xxx
```
返回三维度分类数据：
- **byLocation**: 按地点分组统计（如"黄山 15条"）
- **byType**: 按媒体类型分组统计（VR_MEDIA/IMAGE/AUDIO等）
- **byTime**: 按月份分组统计

#### 内容层级提升
```
POST /api/posts/:id/promote
Body: { targetLevel: 'ESSAY', content: '...', title: '我的黄山游记' }
```
将内容从当前层级提升到目标层级，自动创建新记录并通过 `parent_post_id` 关联上级内容。提升到 ESSAY 级别时自动创建 Journey 记录。

### 3.3 前端页面

#### 探索发现 (`/explore`) ✅ — 合并原瞬间捕获功能
- 按内容类型筛选：全部 / 第一视角 / 影像 / 语音 / 游记 / 随记 / 瞬间
- 按排序切换：热门 / 最新 / 精选
- 热门话题标签筛选
- 热门创作者推荐

#### 内容分类 (`/classified`) ✅
- 三维度分类视图：按地点 / 按类型 / 按时间
- 卡片网格展示分类结果，点击进入该分类内容列表
- 选择分类后，使用 HierarchyList 按维度过滤内容

#### 我的日记 (`/diaries`) ✅
- 个人随笔空间，带有私密感设计
- 私密提示条："日记默认为私密内容，仅自己可见"
- 使用 HierarchyList 展示当前用户的 DIARY 级别内容
- 详情页支持查看/编辑/删除

#### 游记散文 (`/journeys`) ✅
- 已发布/私密两个Tab切换
- 详情页支持查看/编辑/发布/撤回
- 发布时支持地点精度控制

### 3.4 TypeScript类型定义

```typescript
// 前端 (web/src/types/index.ts)
export type ContentLevel = 'SNAPSHOT' | 'CLASSIFIED' | 'DIARY' | 'ESSAY';

export const ContentLevelLabel: Record<ContentLevel, string> = {
  SNAPSHOT: '瞬间捕获',
  CLASSIFIED: '内容分类',
  DIARY: '日记',
  ESSAY: '游记',
};

export interface Post {
  // ... 原有字段 ...
  contentLevel: ContentLevel;      // 内容层级
  parentPostId?: string | null;    // 上级内容ID
  parentPost?: Post | null;        // 上级内容引用
}
```

---

## 四、第一版开发计划：四层内容闭环

> 目标：先实现四层内容体系的基础闭环（手动创建+编辑），AI生成放在后续版本。

### 4.1 ② 内容分类 — 完善分类过滤 ✅

**问题：** `/classified` 页面选择分类后，内容列表硬编码为 `postType="VR_MEDIA"`，没有真正按维度过滤。

| 任务 | 文件 | 说明 |
|---|---|---|
| 后端：按维度过滤API | `server/src/modules/posts/posts.controller.ts` / `posts.service.ts` | `GET /api/posts/hierarchy?level=SNAPSHOT&location=黄山&mediaType=IMAGE&month=2026-07` |
| 前端：分类结果联动 | `web/src/app/(main)/classified/page.tsx` | 使用 HierarchyList 组件，选择分类后按维度过滤 |

### 4.2 ③ 日记/日志 — 个人随笔空间 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 写日记入口 | `web/src/app/(main)/upload/page.tsx` | 新增"写日记"Tab，支持地点/话题/多图 ✅ |
| 日记发布时自动设置层级 | `server/src/modules/posts/posts.service.ts` | 创建时 `contentLevel='DIARY'`，`visibility='PRIVATE'` ✅ |
| 我的日记列表 | `web/src/app/(main)/diaries/page.tsx` | 使用 HierarchyList，点击卡片进入详情 ✅ |
| 日记详情页 | `web/src/app/(main)/diaries/[id]/page.tsx` | 查看/编辑/删除，权限校验仅自己可见 ✅ |

### 4.3 ④ 游记/散文 — 核心功能 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 游记创作器 | `web/src/app/(main)/upload/journey-creator/page.tsx` | 已有，待优化 |
| 游记数据结构 | `server/src/entities/post.entity.ts` | 已有 JOURNEY 类型 + journeys 表 ✅ |
| 游记草稿保存 | `server/src/modules/posts/posts.service.ts` | `contentLevel='ESSAY'`，`visibility='PRIVATE'` ✅ |
| 我的游记列表 | `web/src/app/(main)/journeys/page.tsx` | 已发布/私密两个Tab，点击卡片进入详情 ✅ |
| 游记详情页 | `web/src/app/(main)/journeys/[id]/page.tsx` | 查看/编辑/删除/发布/撤回，地点精度控制 ✅ |
| 发布到社区 | `server/src/modules/posts/posts.controller.ts` | `POST /api/posts/:id/publish` + `unpublish` ✅ |

### 4.4 发布与可见范围控制 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 发布/撤回API | `server/src/modules/posts/posts.controller.ts` | `POST /api/posts/:id/publish` `POST /api/posts/:id/unpublish` ✅ |
| 地点精度控制 | `server/src/modules/posts/posts.service.ts` | publish时支持 hidden/city/exact 三种精度 ✅ |
| 私密内容保护 | `server/src/modules/posts/posts.service.ts` | `PRIVATE` 内容不进入公开列表 ✅ |

### 4.5 第一版开发优先级

```
┌─────────────────────────────────────────────────────────────┐
│  第一梯队（必须先做）                                         │
│  ① 内容分类过滤完善 → ② 日记写/存/列  → ③ 游记手动创作        │
├─────────────────────────────────────────────────────────────┤
│  第二梯队（发布能力）                                         │
│  ④ 发布/撤回API → ⑤ 可见范围控制 → ⑥ 社区展示联动            │
├─────────────────────────────────────────────────────────────┤
│  第三梯队（后续版本）                                         │
│  ⑦ AI游记生成 → ⑧ 内容审核 → ⑨ 微信分享                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、搭子匹配系统实现

### 5.1 匹配算法

```
综合得分 = 目的地匹配(≤40分) + 兴趣重合(≤25分) + 同城(15分) + 共同好友(≤10分) + 近期活跃(5分)
推荐阈值: ≥ 50分
```

| 维度 | 得分规则 | 最高分 |
|---|---|---|
| 相同目的地 | 每共享一个目的地 +20分 | 40分 |
| 共同兴趣标签 | 每重合一个标签 +8分 | 25分 |
| 同城 | 区域完全相同 +15分 | 15分 |
| 共同好友关注 | 每位共同好友 +5分 | 10分 |
| 近期活跃 | 近期发布≥3条内容 +5分 | 5分 |

### 5.2 后端API

```
GET /api/social/recommended/companions?destination=黄山&page=1&limit=20
```

请求流程：
1. 获取当前用户兴趣标签和已访问目的地
2. 排除本人和已关注用户
3. 并行获取候选用户：兴趣标签、共同好友、目的地交集
4. 应用多维评分算法
5. 过滤低于50分的候选人
6. 按得分降序分页返回

返回字段：`matchScore`、`matchReasons`（匹配原因中文描述）、`sharedDestinations`（共享目的地列表）、`commonTags`（共同标签）

### 5.3 前端页面

**找搭子页** (`/discover` → 默认Tab)

- 目的地搜索筛选
- 搭子卡片展示：
  - 头像 + 昵称 + 匹配度百分比
  - 匹配原因标签（去过XX / N个共同兴趣 / 同城 / 共同好友）
  - 共享目的地标签
  - 操作按钮：关注 / 打招呼（快捷私信）

---

## 六、第一版文件变更清单

### 前端 (web/) — 新增/修改文件

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `src/components/layout/sidebar.tsx` | 修改 | 重新设计：合并重复项，4个清晰分区 |
| `src/components/layout/navbar.tsx` | 修改 | 修复 hydration mismatch |
| `src/components/layout/mobile-nav.tsx` | 修改 | 修复 hydration mismatch |
| `src/components/feed/hierarchy-list.tsx` | **新建** | 层级内容列表组件，支持 detailBasePath |
| `src/app/(main)/classified/page.tsx` | 修改 | 使用 HierarchyList 按维度过滤 |
| `src/app/(main)/upload/page.tsx` | 修改 | 新增"写日记"Tab，支持地点/话题/多图 |
| `src/app/(main)/diaries/page.tsx` | 修改 | 我的日记列表，点击进入详情 |
| `src/app/(main)/diaries/[id]/page.tsx` | **新建** | 日记详情页：查看/编辑/删除 |
| `src/app/(main)/journeys/page.tsx` | 修改 | 已发布/私密两个Tab，点击进入详情 |
| `src/app/(main)/journeys/[id]/page.tsx` | **新建** | 游记详情页：查看/编辑/发布/撤回 |
| `src/lib/post-api.ts` | 修改 | 新增层级查询、发布/撤回接口 |

### 后端 (server/) — 新增/修改文件

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `src/modules/posts/posts.controller.ts` | 修改 | 新增按维度过滤、发布/撤回端点 |
| `src/modules/posts/posts.service.ts` | 修改 | 内容层级过滤、地点精度控制、发布状态控制 |

---

## 七、待上线操作

### 7.1 数据库迁移

```bash
# 在MySQL中执行（如尚未执行）
mysql -u root -p xuxiake < server/sql/migrate-content-hierarchy.sql
```

### 7.2 验证检查清单

- [ ] 数据库迁移成功（`content_level`和`parent_post_id`字段存在）
- [ ] 后端启动正常，`/api/posts/hierarchy` 接口可访问
- [ ] 侧边栏无重复项，4个分区（浏览发现/我的内容/创作/个人）
- [ ] `/classified` 页面选择分类后，内容列表正确过滤
- [ ] `/upload` 页面"写日记"Tab可正常编辑，支持地点/话题/配图
- [ ] `/diaries` 页面显示当前用户的私密日记，卡片可点击
- [ ] `/diaries/[id]` 详情页可查看/编辑/删除日记
- [ ] `/journeys` 页面已发布/私密两个Tab，卡片可点击
- [ ] `/journeys/[id]` 详情页可查看/编辑/发布/撤回游记
- [ ] 发布对话框支持地点精度选择（隐藏/仅城市/具体地点）
- [ ] 私密内容不出现在社区Feed和搜索中
- [ ] Hydration mismatch 错误已修复

---

## 八、后续版本规划

> 各模块按优先级排序，同一模块内按阶段顺序执行。

---

### 8.1 AI游记生成

| 任务 | 说明 |
|---|---|
| 多模态模型接入 | 接入视觉+语言多模态大模型，理解照片/视频内容 |
| 视频抽帧处理 | 从VR视频中自动抽取关键帧作为生成素材 |
| 结构化输出 | 标题→导语→正文分段→配图建议 结构化生成 |
| 内容层级联动 | SNAPSHOT → DIARY → ESSAY 自动升级管线 |
| 风格选项 | 文艺/攻略/日记/轻松 多种写作风格可选 |
| 人工编辑 | AI生成后可二次编辑修改 |

### 8.2 消息功能（4个Phase）

#### Phase 1 — 基础体验修复

| 任务 | 说明 |
|---|---|
| 合并重复Controller | ChatController 和 ConversationsController 去重合并 |
| WebSocket实时消息 | 前端接入WebSocket替换REST轮询 |
| 修复未读计数 | MobileNav hardcoded "2" → 真实未读数；ChatController 硬编码 unreadCount: 0 → 真实查询 |
| 图片/文件发送UI | 补全聊天中图片和文件发送的前端界面 |

#### Phase 2 — 内容驱动对话

| 任务 | 说明 |
|---|---|
| 富媒体内容卡片 | 笔记/路线/VR/活动 → 结构化预览卡片（messages表新增 card_type + card_data） |
| 内容分享到消息 | 分享按钮 → 选择好友 → 卡片消息 |
| 语音消息 | 按住说话，距离感应切换听筒 |
| 消息反应 | 长按 → 6个预设emoji + 自定义（新增 message_reactions 表） |

#### Phase 3 — 旅行场景增强

| 任务 | 说明 |
|---|---|
| 位置共享 | 发送当前位置 / 实时位置共享（新增 location_shares 表） |
| 活动群组消息 | 围绕社区挑战自动创建群聊，领队广播模式 |
| 行程协作 | 群聊中共同规划路线、标注兴趣点 |
| 已读状态 | 消息已读/未读状态追踪 |

#### Phase 4 — 差异化创新

| 任务 | 说明 |
|---|---|
| 消失消息模式 | 阅后即焚 + 截图检测（conversations 表新增 is_disappearing） |
| VR内容内联预览 | 聊天中直接预览VR全景内容 |
| AI助手消息集成 | AI旅行助手接入消息流 |
| 语音/视频通话 | WebRTC实时通话 |

### 8.3 AR媒体功能（P2-P3）

#### P2 — 沉浸体验与社交互动

| 任务 | 说明 | 技术方案 |
|---|---|---|
| 旅行轨迹AR回放 | 3D地球轨迹可视化，时间线照片墙，AR标记点 | Three.js/CesiumJS + GPS数据 |
| 视频弹幕/评论 | 时间轴评论（类似B站），音频评论（语音回复） | 新增 video_comments 表 |
| AR内容地图层 | 在地图上显示附近AR内容标记 | 地图SDK + AR标记 |
| 热门AR排行榜 | 热门AR视频/图片/音频排行 | — |
| AR内容话题聚合 | #日出 #星空 #徒步 等话题聚合 | — |
| 音频专辑系统 | 创建专辑、章节排序、连续播放、定时关闭 | 新增 audio_playlists 表 |
| 内容合集 | 多AR视频/图片编入合集展示 | — |
| AR截图/录屏分享 | 一键截图当前AR视图并分享 | — |

**数据库变更**：新增 `video_comments`、`audio_playlists` 表；posts 表新增 `ar_metadata JSON`

**新建页面**：`/albums`、`/albums/:id`、`/ar-map`

#### P3 — 差异化创新

| 任务 | 说明 | 技术方案 |
|---|---|---|
| AR实时特效 | 实时AR滤镜、地理AR标记、天气/时间叠加 | WebXR Device API + 8th Wall/AR.js |
| AI辅助创作 | 自动字幕、智能剪辑、音频降噪、封面推荐、旅行摘要 | 多模态AI模型 |
| 协作媒体 | 多人协作视频、协作音频导览、媒体请求 | 新增 collaborative_media、media_requests 表 |
| 离线与导出 | 离线下载、GPX导出、视频导出MP4、打印明信片 | — |

### 8.4 社群功能（Phase 3-4）

#### Phase 3 — 管理与发现

| 任务 | 说明 |
|---|---|
| 社群设置页面 | 编辑社群信息（封面/名称/简介/规则/分类） |
| 成员管理 | 查看成员列表、分配角色（管理员/版主）、踢出成员 |
| 挑战创建UI | 管理员创建挑战表单（名称/描述/规则/时间/奖励） |
| 社群搜索发现 | 全局搜索社群 + 话题聚合 + 热门社群排行榜 |

#### Phase 4 — 差异化

| 任务 | 说明 | 借鉴来源 |
|---|---|---|
| 社群地图 | 旅行轨迹标记、成员打卡地点热力图、推荐路线展示 | Polarsteps、AllTrails |
| 社群攻略/路线 | 共享攻略文档、协作编辑VR路线 | 马蜂窝、Komoot |
| 社群相册/精选 | VR截图合集、管理员策展精选内容 | Unsplash集合 |

### 8.5 推荐社区算法（P1-P3）

#### P1 — 行为信号采集 + 负反馈优化

| 任务 | 说明 |
|---|---|
| 行为数据采集 | 新增 user_community_interactions 表，记录浏览/点赞/评论行为 |
| 行为信号纳入评分 | 行为信号占20分权重 |
| 负反馈持久化 | 从内存迁移到 recommendation_feedback 表 |
| 反馈学习 | 负反馈标签降权，正反馈标签升权 |

#### P2 — 发现体验增强

| 任务 | 说明 |
|---|---|
| 分类筛选 | 旅行/VR/活动/文化 分类过滤 |
| 无限滚动分页 | 推荐列表懒加载分页 |
| 内容质量评分 | 社区帖子平均互动率，占15分权重 |
| 刷新推荐按钮 | 一键获取新推荐结果 |

#### P3 — 高级推荐

| 任务 | 说明 |
|---|---|
| 协同过滤 | "相似用户还加入了..." 推荐 |
| 实时推荐更新 | 新帖/新成员触发重排序 |
| Explore页社区入口 | Explore页面增加社区发现入口 |
| 社区搜索API | 搜索页接入真实社区搜索 |

### 8.6 内容审核 ✅

| 任务 | 说明 |
|---|---|
| 文本敏感词过滤 | ✅ 敏感词列表 + 隐私信息检测（身份证/手机号/车牌） |
| 审核队列 | ✅ content_reviews 表 + approve/reject API |
| 举报处理 | ✅ ReportDialog 前端 + reports 表 + resolve API |
| 图片审核 | 后续接入云服务（阿里云内容安全/腾讯云） |
| 分级管理 | 后续版本 |
| 申诉机制 | 后续版本 |

**新增文件：**
- 后端：`content-review.entity.ts`、`report.entity.ts`、`review.service.ts`
- 前端：`report-dialog.tsx`、PostCard 集成
- SQL：`migrate-content-review.sql`

### 8.7 微信分享

| 任务 | 说明 |
|---|---|
| 系统分享面板 | 调用微信JS-SDK，唤起分享面板 |
| 海报生成 | 服务端渲染分享海报（含二维码+封面+标题） |
| 分享追踪 | 记录分享来源、转化率、回流数据 |
| 朋友圈分享 | 支持分享到朋友圈（图片+文案） |
| 小程序跳转 | 微信内打开H5页面引导跳转小程序 |

### 8.8 基础设施

| 任务 | 说明 |
|---|---|
| 对象存储迁移 | ✅ StorageService 适配层（local/oss/cos），通过 STORAGE_TYPE 切换 |
| 图片压缩/转码 | ✅ sharp 缩略图生成（400x300, JPEG 80%） |
| 视频转码 | 后续版本 |
| API限流 | 后续版本 |
| 日志与监控 | 后续版本 |

---

## 九、整体优先级排序

```
第一梯队（近期）: 第一版四层内容闭环 ✅ → 消息P1 ✅ → 内容审核 ✅ → 对象存储 ✅
第二梯队（中期）: 消息P2 ✅ → 社群P3 ✅ → 推荐P1 ✅ → AR P2 → AI游记
第二梯队（中期）: 消息P2 → 社群P3 → 推荐P1 → AR P2 → AI游记
第三梯队（远期）: 消息P3+P4 → 社群P4 → 推荐P2+P3 → AR P3 → 微信分享
```

---

**结论：** 第一版聚焦四层内容体系的基础闭环（分类过滤+日记写存+游记创作+发布控制），验证手动创作流程后再引入AI生成能力。
