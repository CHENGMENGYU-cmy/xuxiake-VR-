# 徐霞客社区建设实现说明

> 基于会议记录"AI时代社交平台"建设意见 + 快闪AI游记需求说明书 + 现有代码基础
> 编制日期：2026-07-27 ｜ 更新：2026-08-16（V1.3 对齐三级内容链条与信息架构收敛）

---

## 一、社区建设路线图

### 整体架构：从"传统社交"到"AI时代第一视角平台"

```
用户（"徐霞客"）→ 眼镜/手机记录 → 三级内容链条 → 社区展示 → 搭子匹配 → 持续互动
                         │              │            │            │
                   闪拍 SNAPSHOT   日记 DIARY   游记 TRAVELOGUE
                   (瞬间捕获·素材)  (个人随笔)   (正式文章)
```

内容链条为 **3 级**（V1.2 起由 4 级精简，CLASSIFIED/LOG 层已彻底移除，ESSAY 散文层级保留并与 TRAVELOGUE 同属游记）：

```
闪拍 SNAPSHOT ──精选+AI写日记──▶ 日记 DIARY ──升华/AI生成──▶ 游记 TRAVELOGUE
   (素材：照片/视频/语音/文字)        (个人随笔·私密)          (图文章节·正式文章)
```

### 实施阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 品牌升级 | 全站UI/文案统一为"第一视角""瞬间捕获"主题 | ✅ 已完成 |
| 三级内容链条 | 闪拍→日记→游记 数据模型 + 后端API + 前端页面 | ✅ 已完成 |
| 素材库 | 按天/地点/时间/行程集合浏览 + 多选 AI 写日记 | ✅ 已完成 |
| AI 日记/游记生成 | 批量生成日记 + 结构化章节式游记生成管线 | ✅ 已完成 |
| 闪拍App接入 | Android"同步到社区" → SNAPSHOT 素材（含行程） | ✅ 已完成 |
| 搭子系统 | 匹配算法 + 推荐API + 找搭子 Tab | ✅ 已完成 |
| 社区首页改版 | 发现/关注双模式 + 类型筛选 | ✅ 已完成 |
| 社群升级 | 动态流 + 社群管理 + 挑战系统 | ⚠️ 部分完成（Phase 1-2 完成，3 部分实现） |
| 内容审核 | 敏感词检测 + 审核队列 + 举报系统 | ✅ 已完成 |
| 微信分享 | 系统分享+海报+JS-SDK | 后续版本 |

---

## 二、品牌升级实现细节

### 2.1 命名对照表

| 原名称 | 新名称 | 出现位置 |
|---|---|---|
| 旅行视频 | 第一视角 / 瞬间捕获 | 侧边栏、素材库、媒体页、探索页 |
| 旅行图片 | 影像集 / 瞬间捕获 | 侧边栏、素材库、媒体页、探索页 |
| 音频记录 | 语音记录 | 侧边栏、素材库、媒体页、搜索建议 |
| 媒体广场 | 第一视角 / 瞬间捕获 | 侧边栏分区、媒体页标题 |
| VR内容 / VR | 第一视角 | 帖子卡片标签、Feed筛选、聊天卡片 |
| 上传内容 | ~~分享见闻~~（已永久移除） | 上传降级为全局弹窗"上传素材" |
| 笔记 | 随记 | 帖子卡片、Feed筛选 |
| 动态 | 瞬间 | 帖子卡片、Feed筛选、探索页 |
| 发现好友 | 找搭子 | 发现页 `/discover` "找搭子"Tab |
| 发布内容（个人主页） | 在路上 | Profile Tab |
| 媒体库（个人主页） | 瞬间捕获 | Profile Tab |
| 赞过的（个人主页） | 收藏 | Profile Tab |
| 旅程 / 游记散文 | 我的游记 | `/journeys` 页面 |
| 闪拍素材 | 素材库 | 侧边栏"我的"区、`/snap` |
| 上传（移动端） | 上传 | 移动端底部导航（打开上传弹窗） |

### 2.2 侧边栏布局

> V1.3（2026-08-16）信息架构收敛：取消"创作"分组与"分享见闻"入口（用户明确永久移除），素材上传降级为全局弹窗（素材库/顶栏/移动端底部"上传"统一打开），写作入口收敛到对应内容页内部。

**普通用户：**
```
┌─────────────────────────┐
│    用户信息              │
├─────────────────────────┤
│ 浏览                    │
│ 首页 / 发现             │
├─────────────────────────┤
│ 我的（三级内容链条）      │  ← 素材→日记→游记
│ 素材库 / 我的日记 / 我的游记│
├─────────────────────────┤
│ 个人                    │
│ 消息 / 通知 / 设置       │
└─────────────────────────┘
```

**管理员/审核员（浏览/我的分组隐藏，展示管理中心）：**
```
┌─────────────────────────┐
│ 用户信息 [管理员]徽章      │  ← 角色身份可视化
├─────────────────────────┤
│ 管理中心 🛡              │  ← 核心工作区
│ 仪表板 / 审核队列 / 举报管理│
│ 用户管理（仅ADMIN）        │
├─────────────────────────┤
│ 个人                    │
│ 消息 / 通知 / 设置       │
└─────────────────────────┘
```

**写作/上传入口统一（无独立"创作"区）：**
- **上传素材**：全局弹窗 `snap-upload-dialog`（素材库"上传素材"按钮 / 顶栏上传 / 移动端底部"上传" / feed发布框统一打开）
- **写日记**：我的日记页弹窗 `DiaryComposeDialog`（先选素材→自己写/AI写）→ `/diaries/new` 编辑器
- **写游记**：我的游记页顶部（手写 `/upload/journey-creator` 与 AI写 `/journeys/generate` 并列）

### 2.3 Feed首页改造

- 新增 **发现/关注** 双模式切换（默认"发现"展示全站公开内容）
- 内容类型筛选标签（已实际渲染）：全部 / 第一视角 / 随记 / 游记 / 瞬间
  - 「随记」Tab 传 `postType=NOTE + excludeContentLevel=DIARY`，不再混入公开日记（日记走日记广场）

---

## 三、三级内容链条实现

### 3.1 数据模型

```sql
-- posts 表字段
content_level ENUM('SNAPSHOT','DIARY','TRAVELOGUE','ESSAY')
    NOT NULL DEFAULT 'SNAPSHOT' COMMENT '内容层级',
parent_post_id VARCHAR(36) NULL COMMENT '上级内容ID',
trip_id VARCHAR(36) NULL COMMENT '所属行程ID',        -- 闪拍App行程分组
trip_title VARCHAR(255) NULL COMMENT '所属行程标题',

-- content_level 说明：
-- SNAPSHOT    闪拍素材：眼镜/手机原始第一视角画面（照片/视频/语音/纯文字）
-- DIARY       日记：内容+个人感想，私密为主（未形成正式文章）
-- TRAVELOGUE  游记：基于闪拍/日记，图文章节式正式游记
-- ESSAY       游记散文：与 TRAVELOGUE 同属游记的旧层级，兼容保留

-- parent_post_id：自引用外键，形成内容升级链路
-- 例如：SNAPSHOT(闪拍素材) → DIARY(带感想的日记) → TRAVELOGUE(正式游记)
-- 注：V1.2 起移除 CLASSIFIED/LOG 层（历史上曾作为内容分类/日志，已彻底清理）
```

### 3.2 后端API

#### 内容层级查询
```
GET /api/posts/hierarchy?level=SNAPSHOT&userId=xxx&cursor=yyy&limit=12
```
按内容层级获取帖子列表，支持游标分页。

#### 分类维度聚合
```
GET /api/posts/classified/dimensions?userId=xxx
```
返回分类维度数据：
- **byLocation**: 按地点分组统计（如"黄山 15条"）
- **byType**: 按媒体类型分组统计（VR_MEDIA/IMAGE/AUDIO等）
- **byTime**: 按月份分组统计
- **byTrip**: 按行程分组统计（行程维度，素材库"按行程"视图）

#### 内容层级提升
```
POST /api/posts/:id/promote
Body: { targetLevel: 'TRAVELOGUE', content: '...', title: '我的黄山游记' }
```
将内容从当前层级提升到目标层级，自动创建新记录并通过 `parent_post_id` 关联上级内容。

#### 发布/撤回
```
POST /api/posts/:id/publish       POST /api/posts/:id/unpublish
```
发布时支持地点精度控制（hidden/city/exact）；PRIVATE 内容不进入公开列表。

#### AI 内容生成
```
POST /api/posts/diary/generate-batch           # N张闪拍 → AI 批量生成一篇日记
POST /api/posts/travelogue/generate            # 闪拍+日记 → AI 生成结构化游记
POST /api/posts/travelogue/generate-by-trip    # 行程内闪拍 → AI 生成游记
GET  /api/posts/.../job/:jobId                 # 任务进度轮询
```

#### 闪拍App同步
```
POST /api/sync/snapshots
POST /api/upload/{image,video,audio}
```
闪拍App（Android）采集数据按 `moment.id`/`originalId` 幂等同步，生成 PRIVATE 的 SNAPSHOT 素材，并携带行程 trip_id/trip_title。

### 3.3 前端页面

#### 素材库 (`/snap`) ✅ — 闪拍素材中心
- 四种视图：全部（按天集合）/ 按地点 / 按时间 / 按行程，集合→照片墙两级浏览
- 照片墙全屏预览、左右滑动；纯文字闪拍以文字卡片展示
- 多选 → 底部操作栏"AI写日记" / "生成游记"（跳 `/journeys/generate?ids=`）
- 顶部"上传素材"按钮打开全局上传弹窗（`snap-upload-dialog`）

#### 发现 (`/discover`) ✅ — 三 Tab 聚合
- 日记广场 / 话题 / 找搭子 三个 Tab（`?tab=` 受控，可刷新直达）
- 找搭子：目的地筛选 + 搭子卡片（匹配度/匹配原因/共享目的地）
- 日记广场：全站公开日记浏览

#### 我的日记 (`/diaries`) ✅
- 个人随笔空间，私密为主："日记默认为私密内容，仅自己可见"
- 列表卡片展示封面/标题/天气/心情；`DiaryComposeDialog` 先选素材→自己写/AI写
- `/diaries/new` 编辑器（手写与 AI 共用，支持本地图+素材库配图混用）
- 详情页支持查看/编辑/删除/升华游记（`?ids={diaryId}`）

#### 我的游记 (`/journeys`) ✅
- 已发布/私密两个Tab切换；顶部手写"写游记" + "AI写游记"并列
- `/upload/journey-creator` 图文章节式编辑器（标题/封面/导语/信息卡/按天章节/结尾感悟）
- `/journeys/generate` AI 生成页（多选闪拍+日记→语气/篇幅→任务轮询→去编辑并发布）
- 详情页查看/编辑/发布/撤回，发布时支持地点精度控制

#### 内容分类 (`/classified`) ⚠️ 孤儿页面
- 三维度分类视图代码保留，但 **无任何导航入口**（V1.2 后侧边栏"日志分类"入口已移除）
- 内容链条精简后该页不再承载主流程，作为历史遗留保留（含 /topics 等聚合页）

### 3.4 TypeScript类型定义

```typescript
// 前端 (web/src/types/index.ts)
export type ContentLevel = 'SNAPSHOT' | 'DIARY' | 'TRAVELOGUE' | 'ESSAY';

export const ContentLevelLabel: Record<ContentLevel, string> = {
  SNAPSHOT: '瞬间捕获',
  DIARY: '日记',
  TRAVELOGUE: '游记',
  ESSAY: '游记',
};

export interface Post {
  // ... 原有字段 ...
  contentLevel: ContentLevel;      // 内容层级
  parentPostId?: string | null;    // 上级内容ID
  parentPost?: Post | null;        // 上级内容引用
  tripId?: string | null;          // 所属行程ID
  tripTitle?: string | null;       // 所属行程标题
}
```

> 注：前端 types 中 CLASSIFIED 类型声明仍有残留（后端已移除），属待清理遗留，不影响主流程。

---

## 四、三级内容链条开发与发布控制

> 已按"闪拍素材→日记→游记"三级链条完成闭环（含手动创作 + AI 生成），详见各子节状态。

### 4.1 闪拍素材 — 素材库与上传 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 素材上传全局弹窗 | `web/src/components/upload/snap-upload-dialog.tsx`（新建） | 素材库/顶栏/移动端底部"上传"统一打开；选本地文件→可选备注→`createPost(PRIVATE, SNAPSHOT)` |
| 素材库视图 | `web/src/app/(main)/snap/page.tsx` | 全部/按地点/按时间/按行程 集合→照片墙两级浏览 |
| 素材可见性 | `server/src/modules/posts/posts.service.ts` | 素材统一 `visibility='PRIVATE'`（仅存素材库），可选公开发布 |
| 行程分组 | `server/src/modules/posts/posts.service.ts` + sync | `trip_id/trip_title` 字段 + byTrip 维度聚合 |

### 4.2 日记 — 个人随笔空间 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 日记创作入口 | `web/src/components/diary/diary-compose-dialog.tsx` | 我的日记页弹窗：先选素材→自己写/AI写 |
| 日记编辑器 | `web/src/app/(main)/diaries/new/page.tsx`（新建） | 手写与 AI 共用，封面图/标题/正文/感悟/心情/天气 |
| AI 批量生成日记 | `web/src/app/(main)/snap/generate/batch/page.tsx` + 后端 `diary/generate-batch` | N张闪拍→DeepSeek 生成一篇日记（关联 sourceSnapIds、复制照片） |
| 我的日记列表/详情 | `web/src/app/(main)/diaries/page.tsx`、`diaries/[id]/page.tsx` | 列表卡片（封面/天气/心情）、详情查看/编辑/删除/升华游记 |
| 日记层级与可见性 | `server/src/modules/posts/posts.service.ts` | 创建时 `contentLevel='DIARY'`，默认 `PRIVATE`，发布可选公开 |

### 4.3 游记 — 核心功能 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 章节式游记编辑器 | `web/src/app/(main)/upload/journey-creator/page.tsx` | 标题/封面/导语/信息卡/按天章节（多图）/结尾感悟，手写与 AI 共用 |
| AI 游记生成页 | `web/src/app/(main)/journeys/generate/page.tsx` | 多选闪拍+日记→语气/篇幅/提示词→任务轮询→去编辑并发布 |
| 游记数据结构 | `server/src/entities/journey.entity.ts` + `journey-stop.entity.ts` + `journey-stop-media.entity.ts` | journeys 扩展 summary/transport/budget/theme/insight + stop_media 多图表 |
| 游记层级 | `server/src/modules/posts/posts.service.ts` | 手写与 AI 统一 `contentLevel='TRAVELOGUE'`，默认 `PRIVATE` |
| 我的游记列表/详情 | `web/src/app/(main)/journeys/page.tsx`、`journeys/[id]/page.tsx` | 已发布/私密两个Tab；详情图文叙事渲染 + 发布/撤回 |

### 4.4 发布与可见范围控制 ✅

| 任务 | 文件 | 说明 |
|---|---|---|
| 发布/撤回API | `server/src/modules/posts/posts.controller.ts` | `POST /api/posts/:id/publish` `POST /api/posts/:id/unpublish` ✅ |
| 地点精度控制 | `server/src/modules/posts/posts.service.ts` | publish时支持 hidden/city/exact 三种精度 ✅ |
| 私密内容保护 | `server/src/modules/posts/posts.service.ts` | `PRIVATE` 内容不进入公开列表 ✅ |
| 素材语义分离 | `server/src/modules/posts/posts.service.ts` | 素材默认 PRIVATE 存素材库；公开→发布到社区，发布后跳转按语义分流 |

### 4.5 开发执行顺序（已完成）

```
┌─────────────────────────────────────────────────────────────┐
│  第一梯队（内容生产）                                         │
│  ① 素材库+上传弹窗 → ② 日记写/存/列 → ③ 章节式游记创作        │
├─────────────────────────────────────────────────────────────┤
│  第二梯队（AI 生成能力）                                      │
│  ④ AI批量写日记 → ⑤ AI结构化游记 → ⑥ 闪拍App同步+行程        │
├─────────────────────────────────────────────────────────────┤
│  第三梯队（后续版本）                                         │
│  ⑦ 微信分享 → ⑧ 图片审核 → ⑨ 运营数据看板                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、搭子匹配系统实现

### 5.1 匹配算法

```
综合得分 = 目的地匹配(≤40分) + 兴趣重合(≤25分) + 同城(15分) + 共同好友(≤10分) + 近期活跃(5分)
推荐阈值: ≥ 50分
```

| 维度 | 得分规则 | 最高分 |
|---|---|---|
| 相同目的地 | 每共享一个目的地 +20分 | 40分 |
| 共同兴趣标签 | 每重合一个标签 +8分 | 25分 |
| 同城 | 区域完全相同 +15分 | 15分 |
| 共同好友关注 | 每位共同好友 +5分 | 10分 |
| 近期活跃 | 近期发布≥3条内容 +5分 | 5分 |

### 5.2 后端API

```
GET /api/social/recommended/companions?destination=黄山&page=1&limit=20
```

请求流程：
1. 获取当前用户兴趣标签和已访问目的地
2. 排除本人和已关注用户
3. 并行获取候选用户：兴趣标签、共同好友、目的地交集
4. 应用多维评分算法
5. 过滤低于50分的候选人
6. 按得分降序分页返回

返回字段：`matchScore`、`matchReasons`（匹配原因中文描述）、`sharedDestinations`（共享目的地列表）、`commonTags`（共同标签）

### 5.3 前端页面

**发现页** (`/discover`，三 Tab：日记广场 / 话题 / 找搭子，`?tab=` 受控可直达)

**找搭子 Tab**（`discover/companions-tab.tsx`）

- 目的地搜索筛选
- 搭子卡片展示：
  - 头像 + 昵称 + 匹配度百分比
  - 匹配原因标签（去过XX / N个共同兴趣 / 同城 / 共同好友）
  - 共享目的地标签
  - 操作按钮：关注 / 打招呼（快捷私信）

---

## 六、当前文件变更清单（V1.3）

### 前端 (web/) — 三级内容链条相关

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `src/components/layout/sidebar.tsx` | 修改 | 三组结构（浏览/我的/个人）+ 管理中心独立组 |
| `src/components/layout/mobile-nav.tsx` | 修改 | 底部导航：首页/发现/上传/消息/我的 |
| `src/components/upload/snap-upload-dialog.tsx` | **新建** | 全局"上传素材"弹窗（替代 /upload 分享见闻页） |
| `src/components/diary/diary-compose-dialog.tsx` | **新建** | 写日记引导弹窗：先选素材→自己写/AI写 |
| `src/components/feed/hierarchy-list.tsx` | **新建** | 层级内容列表组件 |
| `src/app/(main)/snap/page.tsx` | 修改 | 素材库：全部/地点/时间/行程集合→照片墙，多选AI生成 |
| `src/app/(main)/snap/generate/batch/page.tsx` | **新建** | AI 批量生成日记 |
| `src/app/(main)/diaries/page.tsx` | 修改 | 我的日记列表（封面/天气/心情卡片） |
| `src/app/(main)/diaries/[id]/page.tsx` | **新建** | 日记详情：查看/编辑/删除/升华游记 |
| `src/app/(main)/diaries/new/page.tsx` | **新建** | 日记编辑器（手写与 AI 共用） |
| `src/app/(main)/journeys/page.tsx` | 修改 | 我的游记列表（已发布/私密）+ 手写/AI写入口 |
| `src/app/(main)/journeys/[id]/page.tsx` | **新建** | 游记详情：图文叙事渲染/发布/撤回 |
| `src/app/(main)/journeys/generate/page.tsx` | **新建** | AI 游记生成页 |
| `src/app/(main)/upload/journey-creator/page.tsx` | 修改 | 章节式游记编辑器 |
| `src/app/(main)/discover/page.tsx` | 修改 | 三 Tab：日记广场/话题/找搭子 |
| `src/app/(main)/classified/page.tsx` | 保留 | 孤儿页面（无导航入口，历史遗留） |
| `src/app/(main)/upload/page.tsx` | **删除** | "分享见闻"页永久移除（含 7 个孤儿上传组件） |
| `src/types/index.ts` | 修改 | ContentLevel 精简为 SNAPSHOT/DIARY/TRAVELOGUE/ESSAY |
| `src/lib/post-api.ts` / `snap-api.ts` | 修改 | 层级/发布/生成/同步接口 |

### 后端 (server/) — 新增/修改文件

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `src/modules/posts/posts.controller.ts` | 修改 | 层级查询、发布/撤回、diary/travelogue 生成端点 |
| `src/modules/posts/posts.service.ts` | 修改 | 层级过滤、地点精度控制、素材PRIVATE语义 |
| `src/modules/posts/ai.service.ts` | **新建** | MULTI_DIARY / TRAVELOGUE 结构化生成 |
| `src/modules/sync/sync.service.ts` | 修改 | 闪拍App幂等同步 + 行程归组 + textNote |
| `src/entities/journey.entity.ts` | 修改 | summary/transport/budget/theme/insight |
| `src/entities/journey-stop.entity.ts` | 修改 | day_date + highlights/tips |
| `src/entities/journey-stop-media.entity.ts` | **新建** | 章节多图 |
| `src/modules/posts/post.entity.ts` | 修改 | content_level enum + trip_id/trip_title |
| SQL: `migrate-content-level.sql`、`migrate-travelogue-chapters.sql`、`seed-log-diary-travelogue.sql` | 修改/新增 | 3级链条迁移与种子 |

---

## 七、待上线操作

### 7.1 数据库迁移（V1.3 三级链条）

```bash
# 在MySQL中执行（如尚未执行，按顺序）
mysql -u root -p xuxiake < server/sql/migrate-content-level.sql        # content_level enum → SNAPSHOT/DIARY/TRAVELOGUE/ESSAY
mysql -u root -p xuxiake < server/sql/migrate-travelogue-chapters.sql  # journeys/stops/stop_media 扩展
mysql -u root -p xuxiake < server/sql/migrate-trip.sql                 # posts 增 trip_id/trip_title
# 全新 docker 环境无需手动迁移（docker-compose.yml 已挂载全部迁移脚本）
```

### 7.2 验证检查清单

- [ ] 数据库迁移成功（`content_level` enum 为 SNAPSHOT/DIARY/TRAVELOGUE/ESSAY，无 CLASSIFIED/LOG）
- [ ] 后端启动正常，`/api/posts/hierarchy`、`/api/posts/travelogue/generate` 接口可访问
- [ ] 侧边栏为三组（浏览/我的/个人），无"创作"分组与"分享见闻"入口
- [ ] `/snap` 素材库四种视图（全部/地点/时间/行程）正常，多选可"AI写日记"、"生成游记"
- [ ] 全局"上传素材"弹窗（素材库/顶栏/移动端底部"上传"）可上传图/视频/语音，存入素材库为 PRIVATE
- [ ] `/diaries` 显示私密日记，`DiaryComposeDialog` 先选素材→自己写/AI写，进入 `/diaries/new` 编辑器
- [ ] `/journeys` 已发布/私密两个Tab；手写"写游记"与"AI写游记"入口正常
- [ ] `/journeys/generate` 选闪拍+日记→生成→去编辑器→发布全链路正常
- [ ] `/discover` 三 Tab（日记广场/话题/找搭子）可切换，`?tab=` 直达
- [ ] 发布对话框支持地点精度选择（隐藏/仅城市/具体地点）
- [ ] 素材（PRIVATE）不出现在社区Feed和搜索中
- [ ] feed 类型筛选tab已渲染，「随记」不混入公开日记
- [ ] 闪拍App同步后 `/snap` 素材库可见（含行程分组）

---

## 八、后续版本规划

> 各模块按优先级排序，同一模块内按阶段顺序执行。

---

### 8.1 AI游记生成 ✅ 已完成

| 任务 | 说明 | 状态 |
|---|---|---|
| 多模态模型接入 | 接入 DeepSeek 大模型，理解照片/素材内容 | ✅ |
| 结构化输出 | 标题→导语→信息卡→按天章节（自动配图）→结尾感悟 | ✅ |
| 内容层级联动 | SNAPSHOT 闪拍 / DIARY 日记 → TRAVELOGUE 游记生成管线 | ✅ |
| 批量日记生成 | N张闪拍 → AI 批量生成一篇日记（`diary/generate-batch`） | ✅ |
| 行程生成游记 | 行程内闪拍 → 一键生成游记（`travelogue/generate-by-trip`） | ✅ |
| 风格/篇幅选项 | 语气/篇幅/提示词可配置 | ✅ |
| 人工编辑 | 生成后落回章节式编辑器二次编辑再发布 | ✅ |
| 视频抽帧处理 | 从 VR 视频自动抽关键帧 | 后续增强 |

### 8.2 消息功能（4个Phase）

#### Phase 1 — 基础体验修复

| 任务 | 说明 |
|---|---|
| 合并重复Controller | ChatController 和 ConversationsController 去重合并 |
| WebSocket实时消息 | 前端接入WebSocket替换REST轮询 |
| 修复未读计数 | MobileNav hardcoded "2" → 真实未读数；ChatController 硬编码 unreadCount: 0 → 真实查询 |
| 图片/文件发送UI | 补全聊天中图片和文件发送的前端界面 |

#### Phase 2 — 内容驱动对话

| 任务 | 说明 |
|---|---|
| 富媒体内容卡片 | 笔记/路线/VR/活动 → 结构化预览卡片（messages表新增 card_type + card_data） |
| 内容分享到消息 | 分享按钮 → 选择好友 → 卡片消息 |
| 语音消息 | 按住说话，距离感应切换听筒 |
| 消息反应 | 长按 → 6个预设emoji + 自定义（新增 message_reactions 表） |

#### Phase 3 — 旅行场景增强

| 任务 | 说明 |
|---|---|
| 位置共享 | 发送当前位置 / 实时位置共享（新增 location_shares 表） |
| 活动群组消息 | 围绕社区挑战自动创建群聊，领队广播模式 |
| 行程协作 | 群聊中共同规划路线、标注兴趣点 |
| 已读状态 | 消息已读/未读状态追踪 |

#### Phase 4 — 差异化创新

| 任务 | 说明 |
|---|---|
| 消失消息模式 | 阅后即焚 + 截图检测（conversations 表新增 is_disappearing） |
| VR内容内联预览 | 聊天中直接预览VR全景内容 |
| AI助手消息集成 | AI旅行助手接入消息流 |
| 语音/视频通话 | WebRTC实时通话 |

### 8.3 AR媒体功能（P2-P3）

#### P2 — 沉浸体验与社交互动

| 任务 | 说明 | 技术方案 |
|---|---|---|
| 旅行轨迹AR回放 | 3D地球轨迹可视化，时间线照片墙，AR标记点 | Three.js/CesiumJS + GPS数据 |
| 视频弹幕/评论 | 时间轴评论（类似B站），音频评论（语音回复） | 新增 video_comments 表 |
| AR内容地图层 | 在地图上显示附近AR内容标记 | 地图SDK + AR标记 |
| 热门AR排行榜 | 热门AR视频/图片/音频排行 | — |
| AR内容话题聚合 | #日出 #星空 #徒步 等话题聚合 | — |
| 音频专辑系统 | 创建专辑、章节排序、连续播放、定时关闭 | 新增 audio_playlists 表 |
| 内容合集 | 多AR视频/图片编入合集展示 | — |
| AR截图/录屏分享 | 一键截图当前AR视图并分享 | — |

**数据库变更**：新增 `video_comments`、`audio_playlists` 表；posts 表新增 `ar_metadata JSON`

**新建页面**：`/albums`、`/albums/:id`、`/ar-map`

#### P3 — 差异化创新

| 任务 | 说明 | 技术方案 |
|---|---|---|
| AR实时特效 | 实时AR滤镜、地理AR标记、天气/时间叠加 | WebXR Device API + 8th Wall/AR.js |
| AI辅助创作 | 自动字幕、智能剪辑、音频降噪、封面推荐、旅行摘要 | 多模态AI模型 |
| 协作媒体 | 多人协作视频、协作音频导览、媒体请求 | 新增 collaborative_media、media_requests 表 |
| 离线与导出 | 离线下载、GPX导出、视频导出MP4、打印明信片 | — |

### 8.4 社群功能（Phase 3-4）

#### Phase 3 — 管理与发现

| 任务 | 说明 |
|---|---|
| 社群设置页面 | 编辑社群信息（封面/名称/简介/规则/分类） |
| 成员管理 | 查看成员列表、分配角色（管理员/版主）、踢出成员 |
| 挑战创建UI | 管理员创建挑战表单（名称/描述/规则/时间/奖励） |
| 社群搜索发现 | 全局搜索社群 + 话题聚合 + 热门社群排行榜 |

#### Phase 4 — 差异化

| 任务 | 说明 | 借鉴来源 |
|---|---|---|
| 社群地图 | 旅行轨迹标记、成员打卡地点热力图、推荐路线展示 | Polarsteps、AllTrails |
| 社群攻略/路线 | 共享攻略文档、协作编辑VR路线 | 马蜂窝、Komoot |
| 社群相册/精选 | VR截图合集、管理员策展精选内容 | Unsplash集合 |

### 8.5 推荐社区算法（P1-P3）

#### P1 — 行为信号采集 + 负反馈优化

| 任务 | 说明 |
|---|---|
| 行为数据采集 | 新增 user_community_interactions 表，记录浏览/点赞/评论行为 |
| 行为信号纳入评分 | 行为信号占20分权重 |
| 负反馈持久化 | 从内存迁移到 recommendation_feedback 表 |
| 反馈学习 | 负反馈标签降权，正反馈标签升权 |

#### P2 — 发现体验增强

| 任务 | 说明 |
|---|---|
| 分类筛选 | 旅行/VR/活动/文化 分类过滤 |
| 无限滚动分页 | 推荐列表懒加载分页 |
| 内容质量评分 | 社区帖子平均互动率，占15分权重 |
| 刷新推荐按钮 | 一键获取新推荐结果 |

#### P3 — 高级推荐

| 任务 | 说明 |
|---|---|
| 协同过滤 | "相似用户还加入了..." 推荐 |
| 实时推荐更新 | 新帖/新成员触发重排序 |
| Explore页社区入口 | Explore页面增加社区发现入口 |
| 社区搜索API | 搜索页接入真实社区搜索 |

### 8.6 内容审核 ✅

| 任务 | 说明 |
|---|---|
| 文本敏感词过滤 | ✅ 敏感词列表 + 隐私信息检测（身份证/手机号/车牌） |
| 审核队列 | ✅ content_reviews 表 + approve/reject API |
| 举报处理 | ✅ ReportDialog 前端 + reports 表 + resolve API |
| 图片审核 | 后续接入云服务（阿里云内容安全/腾讯云） |
| 分级管理 | 后续版本 |
| 申诉机制 | 后续版本 |

**新增文件：**
- 后端：`content-review.entity.ts`、`report.entity.ts`、`review.service.ts`
- 前端：`report-dialog.tsx`、PostCard 集成
- SQL：`migrate-content-review.sql`

### 8.7 微信分享

| 任务 | 说明 |
|---|---|
| 系统分享面板 | 调用微信JS-SDK，唤起分享面板 |
| 海报生成 | 服务端渲染分享海报（含二维码+封面+标题） |
| 分享追踪 | 记录分享来源、转化率、回流数据 |
| 朋友圈分享 | 支持分享到朋友圈（图片+文案） |
| 小程序跳转 | 微信内打开H5页面引导跳转小程序 |

### 8.8 基础设施

| 任务 | 说明 |
|---|---|
| 对象存储迁移 | ✅ StorageService 适配层（local/oss/cos），通过 STORAGE_TYPE 切换 |
| 图片压缩/转码 | ✅ sharp 缩略图生成（400x300, JPEG 80%） |
| 视频转码 | 后续版本 |
| API限流 | 后续版本 |
| 日志与监控 | 后续版本 |

---

## 九、用户角色与权限体系

### 9.1 系统角色定义

| 角色 | 值 | 说明 |
|---|---|---|
| 普通用户 | `USER` | 默认角色，所有人注册后即为普通用户 |
| 审核员 | `MODERATOR` | 内容审核权限，管理举报和处理违规内容 |
| 系统管理员 | `ADMIN` | 最高权限，用户管理、系统配置、数据审计 |

### 9.2 各角色权限矩阵

#### 普通用户 (USER)

| 功能模块 | 状态 | 说明 |
|---|---|---|
| 浏览公开内容（首页/探索/话题） | ✅ 已实现 | FeedList + 探索发现页 |
| 发布内容（第一视角/影像/语音/随记/瞬间） | ✅ 已实现 | `/upload` 分享见闻 |
| 写日记（私密保存） | ✅ 已实现 | `/diaries` + HierarchyList |
| 写游记（手动创作+编辑） | ✅ 已实现 | `/journeys/generate` + 创作器 |
| AI 游记生成 | ✅ 已实现 | `/journeys/generate` AI 生成页 |
| 发布到社区（含地点精度控制） | ✅ 已实现 | publish/unpublish API |
| 内容分类（按地点/类型/时间） | ✅ 已实现 | `/classified` |
| 点赞/评论/分享 | ✅ 已实现 | PostCard + CommentSection |
| 关注/取关用户 | ✅ 已实现 | social API |
| 加入/退出社群 | ✅ 已实现 | communities API |
| 社群内发布动态 | ✅ 已实现 | CommunityPostComposer |
| 私信聊天（文字/图片/语音/位置/反应） | ✅ 已实现 | `/messages` + WebSocket |
| 举报内容 | ✅ 已实现 | ReportDialog |
| 编辑个人资料 | ❌ 未实现 | 需新增 `/settings/profile` 页面 |
| 数据导出 | ❌ 未实现 | 需新增导出 API（JSON/PDF） |
| 账号注销 | ❌ 未实现 | 需新增注销流程 |

#### 审核员 (MODERATOR)

| 功能模块 | 状态 | 说明 |
|---|---|---|
| USER 全部权限 | ✅ | 继承所有普通用户权限 |
| 审核队列查看 | ✅ 已实现 | `/admin/reviews` + `GET /reviews/queue` |
| 审核通过/驳回 | ✅ 已实现 | approve/reject API + checkAdmin 鉴权 |
| 举报列表查看 | ✅ 已实现 | `GET /reports/list` |
| 举报处理（解决/驳回） | ✅ 已实现 | resolveReport API |
| 内容下架 | ✅ 已实现 | `POST /api/posts/:id/force-unpublish` 强制下架 |
| 批量审核 | ✅ 已实现 | 审核队列支持勾选+批量通过/驳回 |

#### 系统管理员 (ADMIN)

| 功能模块 | 状态 | 说明 |
|---|---|---|
| MODERATOR 全部权限 | ✅ | 继承所有审核员权限 |
| 用户列表查询 | ❌ 未实现 | 需新增 `/admin/users` 管理页 |
| 用户角色分配 | ❌ 未实现 | 需新增角色修改 API |
| 用户封禁/解封 | ✅ 已实现 | `users.status` 字段 + ban/unban API + 登录拦截 |
| 系统配置管理 | ❌ 未实现 | 需新增系统配置表 + 管理页 |
| 运营数据看板 | ❌ 未实现 | 需新增统计 API（用户/内容/活跃度） |
| 审计日志查询 | ❌ 未实现 | 需新增 `audit_logs` 表 + 查询页 |
| 内容专题/精选管理 | ❌ 未实现 | 需新增 `featured` 标记 + 运营页 |

### 9.3 角色管理待实现清单

| 优先级 | 任务 | 角色 | 说明 |
|---|---|---|---|
| P0 | 前端同步 role 字段 | 全部 | ✅ User 类型已新增 role，侧边栏显示角色徽章 |
| P1 | `/admin/users` 用户管理页 | ADMIN | ✅ 用户列表、搜索、角色分配 |
| P1 | 角色修改 API | ADMIN | ✅ `PUT /api/users/:id/role` + `GET /api/users/list` |
| P1 | 管理侧边栏分区 | MOD/ADMIN | ✅ 独立"管理"分区（审核队列/举报管理/用户管理） |
| P1 | `/admin/reports` 举报管理页 | MODERATOR | ✅ 举报列表、查看、处理（解决/驳回） |
| P1 | `/settings/profile` 个人设置页 | USER | ✅ 个人资料/兴趣/隐私/通知/外观 5个Tab |
| P2 | 用户封禁/解封 | ADMIN | ✅ users.status + ban/unban + 登录拦截 |
| P2 | 强制下架内容 | MODERATOR | ✅ force-unpublish API |
| P2 | 批量审核 | MODERATOR | ✅ 审核队列批量操作 |
| P3 | 运营数据看板 | ADMIN | ❌ 用户数/内容数/活跃度统计 |
| P3 | 审计日志 | ADMIN | ❌ audit_logs 表 + 查询页 |
| P3 | 数据导出 | USER | ❌ 个人数据 JSON/PDF 导出 |
| P3 | 账号注销 | USER | ❌ 注销流程 + 数据清理 |

---

## 十、整体优先级排序

```
第一梯队 ✅✅✅✅ (100%)
  四层内容闭环 ✅ → 消息P1 ✅ → 内容审核 ✅ → 对象存储 ✅

第二梯队 ✅✅✅✅✅ (100%)
  消息P2 ✅ → 社群P3 ✅ → 推荐P1 ✅ → AR P2 ✅ → AI游记 ✅

第三梯队（远期）
  消息P3+P4 → 社群P4 → 推荐P2+P3 → AR P3 → 微信分享

角色管理（跨梯队）
  P0: role同步前端 ✅ → P1: 用户管理+角色分配 ✅ → P2: 封禁+批量审核 ✅ → P3: 看板+审计
```

---

**结论：** 第一版聚焦四层内容体系的基础闭环（分类过滤+日记写存+游记创作+发布控制），验证手动创作流程后再引入AI生成能力。
