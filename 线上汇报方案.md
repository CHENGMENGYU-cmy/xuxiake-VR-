# 徐霞客社区开发线上汇报方案

> 会议主题：社区开发阶段进展汇报 + 放假后工作计划部署
> 方案版本：V1.0 · 2026-08-08
> 编制依据：《社区建设实现说明》《内容生成逻辑链条》《项目修改记录 CHANGELOG》

---

## 一、会议基本信息

| 项目 | 内容 |
|---|---|
| 会议主题 | 徐霞客社区开发阶段汇报暨节后工作计划部署 |
| 会议形式 | 线上会议（腾讯会议，发起后邀请全员） |
| 建议时间 | 2026-08-10（周一）14:30，预计 90 分钟（可依团队情况调整） |
| 主持人 | 项目负责人 |
| 记录人 | 会议秘书 / 项目助理（会后产出纪要） |
| 参会人员 | 后端服务、前端 Web、闪拍 App（Android）、测试/QA、产品/需求 |

**会议目标：**
1. 对齐社区开发当前进度，确认"已完成"与"待办"边界；
2. 各模块负责人汇报进展、风险与遗留问题；
3. 部署放假后的工作计划、优先级与责任人。

---

## 二、会议议程

| 时间 | 环节 | 时长 | 负责人 |
|---|---|---|---|
| 14:30–14:35 | 开场：会议目标与议程说明 | 5 min | 项目负责人 |
| 14:35–14:55 | 项目整体进度总览 | 20 min | 项目负责人 |
| 14:55–15:25 | 各模块进展汇报（每模块 5–6 min） | 30 min | 各负责人 |
| 15:25–15:35 | 风险与遗留问题讨论 | 10 min | 全员 |
| 15:35–16:00 | 放假后工作计划部署 | 25 min | 项目负责人 |
| 16:00–16:05 | 责任人与截止时间确认 | 5 min | 项目负责人 |
| 16:05–16:10 | 会议总结与纪要确认 | 5 min | 记录人 |

---

## 三、项目整体进度总览（主持人汇报用）

**已完成的核心链路（约 100%）：**

```
闪拍 App 采集 → 上传媒体 → 同步元数据 → 内容分层 → AI 生成 → 发布 → 内容治理
      ✅               ✅           ✅            ✅        ✅        ✅       ✅
```

| 模块 | 状态 | 关键成果 |
|---|---|---|
| 四层内容体系闭环 | ✅ | 闪拍/分类/日记/游记全链路，品牌升级为"第一视角/瞬间捕获" |
| App 数据同步 | ✅ | `POST /api/sync/snapshots` 幂等同步，Android 端"同步到社区" |
| AI 内容生成 | ✅ | 日记 5 风格真实 DeepSeek；游记结构化章节式（按天分章+配图+信息卡） |
| 发布与可见性 | ✅ | 私密/公开/关注可见 + 地点精度控制 + 软删除/统计口径 |
| 消息功能 | ✅ P1+P2 | WebSocket 实时、未读计数、富媒体卡片、语音、消息反应 |
| 社群功能 | ✅ P0+P1 | 社群空间页、动态发布、公告、挑战参与 |
| 推荐社区 | ✅ P0 | 多信号加权推荐 + 推荐理由展示 |
| AR 媒体 | ✅ P0+P1 | 视频/音频上传、沉浸浏览、媒体预览增强 |
| 内容审核 | ✅ | 敏感词、审核队列、举报处理、强制下架、批量审核 |
| 角色权限 | ✅ P0–P2 | 用户管理、角色分配、封禁、管理侧边栏分区 |
| 测试保障 | ✅ | Playwright 端到端（08/09 spec）+ 接口测试脚本 |

**一句话总结：** 社区从"能看能发"推进到"四层内容 + AI 创作 + 社群互动"的核心闭环全部跑通，接下来进入体验增强与差异化阶段。

---

## 四、各模块工作进展汇报（汇报人逐条讲）

> 建议每人 5–6 分钟，按"做了什么 → 验证结果 → 遗留问题"三段式汇报。

### 4.1 后端服务负责人

**做了什么：**
1. 数据同步链路：`/api/sync/snapshots` 按 `originalId` 幂等去重，媒体走分片上传、元数据走 JSON，接口测试脚本通过；
2. AI 生成升级：游记结构化生成（按素材日期分天章节、自动配当天图、生成标题/导语/信息卡/结尾感悟，落库 `journey + journey_stops + journey_stop_media`）；失败自动回退本地模板；
3. 内容治理：删除改为软删除（`deleted_at`），全部 posts 读查询统一过滤；个人主页统计口径排除草稿/软删/素材；
4. 内容审核：敏感词 + 审核队列 + 举报 + 强制下架 + 批量审核；
5. 消息 P1+P2：合并重复 Controller、修复未读计数、富媒体卡片、语音消息、消息反应；
6. 社群 P0+P1：实体扩展（公告/角色/挑战）、SocialService 服务层抽离、数据库迁移；
7. 角色权限 P0–P2：用户列表/角色分配/封禁接口。

**验证结果：** 全部接口走通，软删后详情 404、列表剔除，统计口径与"我的日记"一致（如 sunqi 帖子数 7）。

**遗留问题：** 图片审核仍为后续接入；视频转码未做；API 限流与监控未做。

### 4.2 前端 Web 负责人

**做了什么：**
1. 四层内容页面：素材库（按地点/时间/行程分类 + 多选跨月）、内容分类、我的日记（草稿箱/公开/私密/关注可见三态切换）、游记散文；
2. 章节式游记编辑器：手写与 AI 统一——标题+封面+导语+信息卡+按天章节（多图/从素材库选图）+结尾感悟，编辑可整体回填；
3. 写日记引导：借鉴小红书"先选素材再进编辑器"，SnapPicker + DiaryComposeDialog 两步式统一入口；
4. 社群空间页重构（封面头部 + 5 Tab）、CommunityPostComposer、公告/挑战 UI；
5. 推荐社区推荐理由展示、内容审核前端（举报弹窗、审核队列批量操作）；
6. 个人主页：按 contentLevel 分类（在路上=日记/游记）、Tab 状态 URL 记忆、粉丝/关注分页；
7. 品牌升级 UI 统一（第一视角/瞬间捕获/找搭子等全站文案）。

**验证结果：** Playwright 端到端 08/09 spec 全链路通过（素材库→写日记→存草稿→草稿箱→发布、手写→发布→详情→编辑回填）。

**遗留问题：** 手写日记与 AI 日记格式已统一；剩余主要依赖后端 P3 能力（运营看板、审计等前端页）。

### 4.3 闪拍 App（Android）负责人

**做了什么：**
1. "同步到社区"：`CommunitySyncManager` 同步协调（登录/上传/幂等/token 刷新）、`CommunityBackendClient` 社区 API 客户端（验证码登录/上传媒体/sync）、`CommunityTokenStore` token 与已同步 id 按账号隔离持久化；
2. 入口：主界面"同步到社区"按钮 → 账号+密码+图形验证码登录 → 手动触发同步；媒体（图片/视频/音频）分片上传 + 元数据提交，二次同步幂等跳过。

**验证结果：** 与后端联调通过，重复同步不重复插入。

**遗留问题：** 当前为手动触发，自动/后台同步、失败断点续传待做；网页导入工具（可选入口）未做。

### 4.4 测试/QA

**做了什么：**
1. Playwright 端到端：`08-snap-library.spec.ts`（素材库 14 步断言：菜单入口/分类 Tab/集合打开/全屏预览/多选/AI 写日记）、`09-diary-flow.spec.ts`（多选→批量编辑→存草稿→草稿箱→发布全链路）；
2. 接口测试：`scripts/test-sync-snapshots.js` 验证同步幂等；
3. 回归验证：软删除、统计口径、章节式游记编辑回填（Playwright 验证 AI 游记编辑页全字段回填）。

**验证结果：** 新增用例全通过，后续改动可一键回归。

**遗留问题：** 移动端（App 侧）自动化测试未建立；图片审核依赖云服务。

### 4.5 产品/需求

**做了什么：**
1. 需求文档：《快闪 AI 游记与社区系统需求说明书 V1.0》、《内容生成逻辑链条 V1.1》（数据采集→同步→分层→AI 生成→发布→治理完整链路）；
2. 设计方案：社群（群聊升级为内容社区）、消息（"内容驱动对话"四 Phase）、AR 媒体（四 Phase）、推荐社区（P0–P3）均已成文并驱动开发。

**验证结果：** 开发与需求对齐，链路文档与代码实现一致。

**遗留问题：** 需补充运营侧需求（数据看板指标口径、内容专题策展）。

---

## 五、风险与遗留问题讨论（全员）

| 风险/问题 | 影响 | 应对 |
|---|---|---|
| 图片审核未接入云服务 | 图片内容缺乏自动拦截 | 放假后接入阿里云/腾讯云内容安全 |
| App 同步为手动触发 | 用户需主动同步，链路完整但体验一般 | 后续做自动同步/后台同步 |
| API 限流/监控缺失 | 上线后无流量保护与告警 | 划入基础设施待办 |
| 移动端无自动化测试 | App 回归靠人工 | 建立 Android 基础冒烟脚本 |
| 运营数据看板/审计日志未做 | 管理员缺数据抓手 | 放假后角色管理 P3 排期 |

---

## 六、放假后工作计划部署

### 总体安排（按优先级分 4 周推进）

| 周次 | 重点模块 | 负责人 | 里程碑/产出 |
|---|---|---|---|
| 第 1 周 | 推荐社区 P1 + 社群 P3 | 后端+前端 | 行为信号采集/负反馈持久化；社群设置页、成员管理、搜索发现 |
| 第 2 周 | 消息 P3 + 推荐 P2 | 后端+前端 | 位置共享、活动群组、已读状态；分类筛选、无限滚动、刷新推荐 |
| 第 3 周 | AR P2 + 社群 P4 | 前端为主 | 旅行轨迹 AR 回放、AR 地图层、音频专辑；社群地图/攻略 |
| 第 4 周 | 审核补强 + 运营 + 微信分享 | 后端+前端 | 图片审核接入；运营数据看板、审计日志；微信分享（系统分享+海报） |

> 备注：各周任务可并行、可动态调整，最终以周会确认为准。角色与分工可依据实际团队调整。

### 6.1 第一周：推荐社区 P1 + 社群 P3

**推荐社区 P1（行为信号采集 + 负反馈优化）**
- 新增 `user_community_interactions` 表，记录浏览/点赞/评论行为；
- 行为信号纳入评分（20 分权重）；
- 负反馈从内存迁移到 `recommendation_feedback` 表；
- 反馈学习：负反馈标签降权、正反馈标签升权。

**社群 P3（管理与发现）**
- 社群设置页面（编辑封面/名称/简介/规则/分类）；
- 成员管理（成员列表/分配管理员·版主/踢出）；
- 社群搜索发现（全局搜索 + 话题聚合 + 热门排行）。

### 6.2 第二周：消息 P3 + 推荐 P2

**消息 P3（旅行场景增强）**
- 位置共享（发送当前位置/实时位置共享，新增 `location_shares` 表）；
- 活动群组消息（围绕社区挑战自动建群，领队广播模式）；
- 行程协作（群聊共同规划路线、标注兴趣点）；
- 已读状态追踪。

**推荐 P2（发现体验增强）**
- 分类筛选（旅行/VR/活动/文化）；
- 无限滚动分页（推荐列表懒加载）；
- 内容质量评分（帖子平均互动率，15 分权重）；
- 刷新推荐按钮。

### 6.3 第三周：AR P2 + 社群 P4

**AR P2（沉浸体验与社交互动）**
- 旅行轨迹 AR 回放（3D 地球轨迹 + 时间线照片墙 + AR 标记点，Three.js/CesiumJS）；
- 视频弹幕/评论（时间轴评论）、音频评论；
- AR 内容地图层（附近 AR 内容标记）；
- 音频专辑系统（专辑/章节排序/连续播放/定时关闭）。

**社群 P4（差异化）**
- 社群地图（成员打卡热力图、推荐路线）；
- 社群攻略/路线（共享攻略文档、协作编辑 VR 路线）。

### 6.4 第四周：审核补强 + 运营看板 + 微信分享

- 图片审核接入云服务（阿里云/腾讯云内容安全）；
- 运营数据看板（用户数/内容数/活跃度统计 API + 管理页）；
- 审计日志（`audit_logs` 表 + 查询页）；
- 微信分享：系统分享面板 + 服务端海报生成（含二维码/封面/标题）+ 分享追踪。

### 6.5 收尾交付项（穿插或延期）

| 任务 | 优先级 | 说明 |
|---|---|---|
| 数据导出（JSON/PDF） | P3 | 用户个人数据导出 |
| 账号注销流程 | P3 | 注销 + 数据清理 |
| App 自动/后台同步 | 待定 | 提升 App 侧体验 |
| 网页导入工具 | 可选 | 手动导入入口 |
| API 限流 / 日志监控 | 待定 | 上线前补强 |

---

## 七、会后交付物与准备清单

**会后需要输出：**
1. 会议纪要（含各模块遗留问题清单、责任人、截止时间）——记录人；
2. 放假后 4 周排期表（纳入周会跟踪）——项目负责人。

**会前准备（各汇报人）：**
- 提前 1 天把汇报提纲/数据（验证截图、用例数）发会议群，压缩会议中演示时间；
- 遗留问题先写进提纲，避免会上现场梳理。

**线上会议注意事项：**
- 提前 10 分钟进会，测试麦克风/屏幕共享；
- 汇报时共享屏幕演示关键界面（素材库、章节式编辑器、社群空间、审核队列）；
- 每模块汇报控制在 6 分钟内，超时由主持人提醒。
